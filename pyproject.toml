[project]
name = "m4-benchmarks"
version = "0.1.0"
description = "Python CPU benchmarks for Apple M4"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.27",
    "pandas>=2.0",
    "matplotlib>=3.8",
]

[tool.uv]
managed = true
